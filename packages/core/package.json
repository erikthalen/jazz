{
  "name": "@jazz/core",
  "private": true,
  "version": "0.1.0-beta.2",
  "type": "module",
  "scripts": {
    "dev": "esbuild src/main.css --bundle --minify --outdir=../.. --watch",
    "build": "esbuild src/main.css --bundle --minify --outdir=../.."
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "prettier": {
    "printWidth": 200
  }
}
